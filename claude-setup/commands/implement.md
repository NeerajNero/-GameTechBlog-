---
description: Implement one approved feature or classified tiny task
argument-hint: <approval-or-tiny-classification-file>
---

Follow the command spec in @commands/implement.md exactly, including its Allowed Actions, Forbidden Actions, and Output sections.

Project scope override: this repository is the GameTechGuides static content site (Next.js App Router + MDX in `apps/web/`). Apply the spec through the content-site lens described in @FLOW_README.md and the lane rules in docs/flow-policy.md. Backend, database, Docker, SDK, and mobile steps in the spec do not apply unless a task explicitly reintroduces them.

Input: $ARGUMENTS

If the input above is empty and the spec requires a file, ask which file to use.
